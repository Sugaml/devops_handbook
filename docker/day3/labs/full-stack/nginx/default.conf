server {
    listen 80;
    location / {
        return 200 'Day 3 — web proxy (use /api/ for backend)\n';
        add_header Content-Type text/plain;
    }
    location /api/ {
        proxy_pass http://api:3000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
}
