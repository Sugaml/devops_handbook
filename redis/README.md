# Redis

The hands-on **Redis CLI and operations** track lives in **[rediscli/](../rediscli/)**.

That handbook covers:

- `redis-cli` fundamentals and data structures
- Memory, persistence, and replication
- Cluster, ACLs, and production troubleshooting
- Docker lab stacks on ports 6379–6383

This `redis/` path is a stable alias for navigation. Start here:

→ [Redis CLI for DevOps — 7-Day Handbook](../rediscli/README.md)
